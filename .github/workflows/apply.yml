name: "Terraform-apply"

on:
  pull_request:
    types: [ closed ]

jobs:
  # terraform-module:
  #   name: "Terraform-module"
  #   runs-on: ubuntu-latest
  #   env:	
  #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_KEY_ID }}
  #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRETE_ID }}
  #     AWS_REGION: 'ap-east-1'

  #   steps:
  #     - name: 'Checkout'
  #       uses: actions/checkout@master
  #     - name: 'Terraform Init'
  #       uses: hashicorp/terraform-github-actions@master
  #       with:
  #         tf_actions_version: 1.0.11
  #         tf_actions_subcommand: 'init'
  #         tf_actions_working_dir: './modules/vpc'
  #         tf_actions_comment: true
  #     - name: 'Terraform plan'
  #       uses: hashicorp/terraform-github-actions@master
  #       with:
  #         tf_actions_version: 1.0.11
  #         tf_actions_subcommand: 'plan'
  #         tf_actions_working_dir: './modules/vpc'
  #         tf_actions_comment: true  
  #     - name: 'Terraform apply'
  #       uses: hashicorp/terraform-github-actions@master
  #       with:
  #         tf_actions_version: 1.0.11
  #         tf_actions_subcommand: 'apply'
  #         tf_actions_working_dir: './modules/vpc'
  #         tf_actions_comment: true           
  terraform:
    name: "Terraform"
    needs: terraform-sandbox
    runs-on: ubuntu-latest
    env:	
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRETE_ID }}
      AWS_REGION: 'us-east-2'
      TERRAFORM_ENVIRONMENT: production

    steps:
      - name: 'Checkout'
        uses: actions/checkout@master
        
      - name: 'Terraform Init'
        uses: hashicorp/terraform-github-actions@master
        with:
          tf_actions_version: 0.12.13
          tf_actions_subcommand: 'init'
          tf_actions_working_dir: './environments/$TERRAFORM_ENVIRONMENT'
          tf_actions_comment: true
      - name: "tf_output"
        run: |
          cd ./environments/production
          terraform init
   

      - name: 'Terraform plan'
        uses: hashicorp/terraform-github-actions@master

        with:
          tf_actions_version: 0.12.13
          tf_actions_subcommand: 'plan'
          tf_actions_working_dir: './environments/$TERRAFORM_ENVIRONMENT'
          tf_actions_comment: true  
  
      - name: "tf_output_plan"
        run: | 
          chmod +x ./bin/deploy.sh
          ./bin/deploy.sh -e $TERRAFORM_ENVIRONMENT $AWS_ACCESS_KEY_ID $AWS_SECRET_ACCESS_KEY -o plan                         
      - name: 'Terraform apply'
        uses: hashicorp/terraform-github-actions@master
        with:
          tf_actions_version: 0.12.13
          tf_actions_subcommand: 'apply'
          tf_actions_working_dir: './environments/$TERRAFORM_ENVIRONMENT'
          tf_actions_comment: true            
      - name: "Hello world when it's Monday"
        run: |
          chmod +x ./bin/deploy.sh
          ./bin/deploy.sh -e $TERRAFORM_ENVIRONMENT $AWS_ACCESS_KEY_ID $AWS_SECRET_ACCESS_KEY  -o apply -a                  
  terraform-sandbox:
    name: "sandbox-Terraform"

    runs-on: ubuntu-latest
    env:	
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRETE_ID }}
      AWS_REGION: 'us-east-1'
      TERRAFORM_ENVIRONMENT: sandbox

    steps:
      - name: 'Checkout'
        uses: actions/checkout@master
      - name: 'Terraform Init'
        uses: hashicorp/terraform-github-actions@master
        with:
          tf_actions_version: 0.12.13
          tf_actions_subcommand: 'init'
          tf_actions_working_dir: './environments/$TERRAFORM_ENVIRONMENT'
          tf_actions_comment: true
      - name: "tf_output"
        run: |
          cd ./environments/sandbox
          terraform init          
      - name: 'Terraform plan'
        uses: hashicorp/terraform-github-actions@master
        with:
          tf_actions_version: 0.12.13
          tf_actions_subcommand: 'plan'
          tf_actions_working_dir: './environments/$TERRAFORM_ENVIRONMENT'
          tf_actions_comment: true    
      - name: "tf_output_plan"
        run: | 
          chmod +x ./bin/deploy.sh
          ./bin/deploy.sh -e $TERRAFORM_ENVIRONMENT $AWS_ACCESS_KEY_ID $AWS_SECRET_ACCESS_KEY -o plan                
      - name: 'Terraform apply'
        uses: hashicorp/terraform-github-actions@master
        with:
          tf_actions_version: 0.12.13
          tf_actions_subcommand: 'apply'
          tf_actions_working_dir: './environments/$TERRAFORM_ENVIRONMENT'
          tf_actions_comment: true           
      - name: "tf_output_apply"
        run: |
          chmod +x ./bin/deploy.sh
          ./bin/deploy.sh -e $TERRAFORM_ENVIRONMENT $AWS_ACCESS_KEY_ID $AWS_SECRET_ACCESS_KEY  -o apply -a          